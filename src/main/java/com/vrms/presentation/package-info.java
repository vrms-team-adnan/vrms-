/**
 * Presentation layer: the entry point for user interaction (e.g. a CLI) —
 * it receives input and displays output, and calls Services from the
 * Application layer only.
 * <p>
 * This layer contains no business logic, and never interacts directly
 * with the Persistence or Domain layers.
 * </p>
 */
package com.vrms.presentation;