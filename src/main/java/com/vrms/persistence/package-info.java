/**
 * Persistence layer: responsible for storing and retrieving data
 * (in-memory or SQL) using the Repository/DAO pattern, such as
 * VehicleRepository and RentalRepository.
 * <p>
 * Repository interfaces are defined here so the Application layer can use
 * them without knowing the actual storage implementation details.
 * </p>
 */
package com.vrms.persistence;